# Execution 02, Phase 02/03. Local machine paths and everything the
# Gradle build regenerates deterministically (Article 19: reproducible
# from a clean checkout, never hand-copied or committed as an
# artifact).

# Machine-specific SDK location -- regenerate with:
#   echo "sdk.dir=<androidHome>" > local.properties
local.properties

.gradle/
build/
*.iml
.idea/

# Generated by app/build.gradle.kts's cargoBuildAndroid /
# generateUniffiBindings tasks -- never hand-copied (Article 19), never
# committed.
app/build/rustJniLibs/
app/build/generated/uniffi/
